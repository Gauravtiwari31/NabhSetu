# 30-day run manifest (template)

Do not fill this with invented official figures. After a real collection
window, record the hashes and file checksums the platform already stores.

- Observation start:
- Observation end:
- Method version:
- Basket version / config hash:
- Weights version / source / checksum:
- Index run id / input hash / output hash:
- Coverage of the final monthly point:
- DGCA file name / URL / SHA-256:
- CPI file name / URL / SHA-256:
- Back-test status (`unavailable` / `not_reportable` / `reportable`):
- Notes:
